# Worker LangGraph Agent Dependencies
fastapi>=0.115.0
pydantic>=2.9.0
pydantic-settings>=2.5.0

# LangChain & LangGraph
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langgraph>=0.2.0

# Database & Storage
sqlalchemy>=2.0.30
asyncpg>=0.29.0
psycopg[binary]>=3.2.0
redis>=5.0.0
qdrant-client>=1.12.0,<1.13.0

# Document & OCR
pytesseract>=0.3.10
PyMuPDF>=1.24.0
python-docx>=1.1.0
pandas>=2.2.0
openpyxl>=3.1.0
Pillow>=10.0.0

# Embeddings, RAG & Utilities
fastembed>=0.3.0
httpx>=0.27.0
pyyaml>=6.0.0
aiofiles>=24.1.0

# Test runner. Present in the image because the worker container IS the test
# environment: it has Tesseract, PyMuPDF and the pre-staged embedding model, and
# it is air-gapped at runtime so nothing can be pip-installed later.
pytest>=8.3.0
