# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0
python-multipart>=0.0.12

# Database
sqlalchemy>=2.0.30
asyncpg>=0.29.0
psycopg[binary]>=3.2.0
alembic>=1.13.0

# Redis
redis>=5.0.0

# HTTP client (for Ollama + egress checks)
httpx>=0.27.0

# Config / validation
pydantic>=2.9.0
pydantic-settings>=2.5.0
pyyaml>=6.0.0

# Utilities
python-magic>=0.4.27
aiofiles>=24.1.0

# Reading generated .docx deliverables back for inline preview.
# The API only ever READS documents; generation stays in the worker.
python-docx>=1.1.0
